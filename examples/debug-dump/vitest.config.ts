import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    // Enable dump to see transformed files
    // You can also use VITEST_DEBUG_DUMP=true environment variable
    // server: {
    //   debug: {
    //     dump: true,
    //   },
    // },
  },
})
