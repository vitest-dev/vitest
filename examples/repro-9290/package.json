{
  "name": "@vitest/example-repro-9290",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "vitest",
    "test:short": "vitest run short",
    "test:verbose": "vitest run verbose",
    "coverage:short": "vitest run short --coverage",
    "coverage:verbose": "vitest run verbose --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "workspace:*",
    "vite": "latest",
    "vitest": "workspace:*"
  }
}
