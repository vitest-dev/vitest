{
  "name": "@vitest/example-puppeteer",
  "private": true,
  "scripts": {
    "build": "vite build",
    "coverage": "vitest run --coverage",
    "test": "vite build && vitest",
    "test:ui": "vite build && vitest --ui"
  },
  "devDependencies": {
    "@vitest/ui": "latest",
    "puppeteer": "^13.6.0",
    "vite": "latest",
    "vitest": "latest"
  },
  "stackblitz": {
    "startCommand": "npm run test:ui"
  }
}
