{
  "name": "@vitest/example-blob-benchmark",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "benchmark": "./run-benchmark.sh",
    "bench:serialization": "vitest bench demo.bench.ts",
    "generate": "node generate-tests.mjs",
    "test:blob": "vitest run --reporter=blob",
    "merge": "vitest run --merge-reports .vitest-reports --reporter=json"
  },
  "devDependencies": {
    "@ungap/structured-clone": "catalog:",
    "devalue": "catalog:",
    "flatted": "catalog:",
    "vitest": "latest"
  }
}
