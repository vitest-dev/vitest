{
  "name": "@vitest/test-puppeteer",
  "private": true,
  "scripts": {
    "build": "vite build",
    "coverage": "vitest --coverage",
    "test": "vite build && vitest"
  },
  "devDependencies": {
    "puppeteer": "^13.0.0",
    "vite": "^2.7.9",
    "vitest": "workspace:*"
  }
}
