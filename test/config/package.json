{
  "name": "@vitest/test-config",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run --typecheck"
  },
  "devDependencies": {
    "vite": "latest",
    "vitest": "workspace:*"
  }
}
