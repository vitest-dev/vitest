{
  "name": "@vitest/snapshots",
  "private": true,
  "scripts": {
    "test": "pnpm run test:snaps && pnpm run test:update",
    "test:snaps": "vitest run --dir test",
    "test:update": "vitest run -u --dir test-update",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "vitest": "workspace:*"
  }
}
