{
  "name": "@vitest/test-utils",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/utils": "workspace:*",
    "vitest": "workspace:*"
  }
}
