{
  "name": "@vitest/benchmark",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "bench:json": "vitest bench --reporter=json",
    "bench": "vitest bench"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}
