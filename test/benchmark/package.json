{
  "name": "@vitest/benchmark",
  "private": true,
  "scripts": {
    "test": "node test.mjs",
    "bench:json": "vitest bench --reporter=json",
    "bench": "vitest bench",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "execa": "^6.1.0"
  }
}
