{
  "name": "@vitest/benchmark",
  "private": true,
  "scripts": {
    "test": "node --test specs/",
    "bench:json": "vitest bench --reporter=json",
    "bench": "vitest bench"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}
