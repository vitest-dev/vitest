{
  "name": "@vitest/benchmark",
  "private": true,
  "scripts": {
    "test": "node test.mjs",
    "bench:json": "vitest bench --reporter=json",
    "bench": "vitest bench"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}
