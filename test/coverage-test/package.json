{
  "name": "@vitest/test-coverage",
  "private": true,
  "scripts": {
    "test": "vitest --coverage",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}
