{
  "name": "@vitest/test-core",
  "private": true,
  "scripts": {
    "test": "vitest --passWithNoTests",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}