{
  "name": "@vitest/test-core",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "tinyspy": "^1.0.2",
    "vitest": "workspace:*"
  }
}
