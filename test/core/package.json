{
  "name": "@vitest/test-core",
  "private": true,
  "scripts": {
    "test": "vitest test/dom.test.ts",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}