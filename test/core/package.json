{
  "name": "@vitest/test-core",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "report": "vite preview --outDir html"
  },
  "devDependencies": {
    "tinyspy": "^1.0.2",
    "vitest": "workspace:*"
  }
}
