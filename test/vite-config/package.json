{
  "name": "@vitest/test-vite-config",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "pathe": "^0.2.0",
    "vitest": "workspace:*"
  }
}
