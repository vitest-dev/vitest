{
  "name": "@vitest/test-vite-config",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "pathe": "^1.1.0",
    "vitest": "workspace:*"
  }
}
