{
  "scripts": {
    "test": "vitest",
    "types": "vitest --typecheck --run",
    "tsc": "tsc --watch --pretty false --noEmit"
  },
  "dependencies": {
    "vitest": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vue-tsc": "^1.8.20"
  }
}
