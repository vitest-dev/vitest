{
  "name": "@vitest/test-cli",
  "private": true,
  "scripts": {
    "test": "vitest --exclude fixtures/exclude/**/string.test.ts"
  },
  "devDependencies": {
    "vite": "latest",
    "vitest": "workspace:*"
  }
}
