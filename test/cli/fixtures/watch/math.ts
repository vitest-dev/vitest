// Modified by file-watching.test.ts
// Modified by file-watching.test.ts
export function sum(a: number, b: number) {
  return a + b
}

console.log("New code running"); // This is used to check that edited changes are actually run, and cached files are not run instead
  
console.log("New code running"); // This is used to check that edited changes are actually run, and cached files are not run instead
  