{
  "name": "@vitest/test-core",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest",
    "build": "vite build",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}
