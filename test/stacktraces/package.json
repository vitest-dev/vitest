{
  "name": "@vitest/test-fails",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "execa": "^6.1.0",
    "vitest": "workspace:*"
  }
}
