{
  "name": "@vitest/test-global-setup",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/web-worker": "workspace:*",
    "vitest": "workspace:*"
  }
}
