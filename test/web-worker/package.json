{
  "name": "@vitest/test-global-setup",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/web-worker": "workspace:*",
    "vitest": "workspace:*"
  }
}
