{
  "name": "@vitest/test-cwd",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "cd $(pwd)/.cache/projects/test && vitest run"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}
