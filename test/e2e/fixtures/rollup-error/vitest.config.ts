import { defineConfig } from "vitest/config";

// pnpm -C test/e2e test -- --root fixtures/rollup-error --environment happy-dom
// pnpm -C test/e2e test -- --root fixtures/rollup-error --environment node

export default defineConfig({})
