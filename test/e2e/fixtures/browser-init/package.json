{
  "scripts": {
    "test:browser": "vitest --config=vitest.browser.config.ts"
  },
  "dependencies": {
    "vitest": "workspace:*"
  },
  "devDependencies": {
    "@vitest/browser-preview": "workspace:*"
  }
}
