{
  "name": "@vitest/test-globalSetup",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest --coverage"
  },
  "devDependencies": {
    "vitest": "workspace:*"
  }
}
