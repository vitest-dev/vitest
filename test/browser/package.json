{
  "name": "@vitest/test-browser",
  "private": true,
  "scripts": {
    "test": "node test.mjs",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/browser": "workspace:*",
    "execa": "^7.1.1",
    "safaridriver": "^0.0.4",
    "vitest": "workspace:*"
  }
}
