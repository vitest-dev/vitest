{
  "name": "@vitest/test-browser",
  "private": true,
  "scripts": {
    "test": "vitest --browser --open",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/browser": "workspace:*",
    "vitest": "workspace:*"
  }
}
