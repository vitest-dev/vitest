{
  "name": "@vitest/test-web",
  "private": true,
  "scripts": {
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/browser": "workspace:*",
    "vitest": "workspace:*"
  }
}
