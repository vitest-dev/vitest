{
  "name": "@vitest/test-browser",
  "private": true,
  "scripts": {
    "test": "vitest run --browser --puppeteer --headless",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/browser": "workspace:*",
    "vitest": "workspace:*"
  }
}
