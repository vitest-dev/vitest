{
  "name": "@vitest/test-browser",
  "private": true,
  "scripts": {
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/browser": "workspace:*",
    "vitest": "workspace:*"
  }
}
