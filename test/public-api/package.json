{
  "name": "@vitest/test-public-api",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest",
    "fixtures": "vitest --root ./fixtures"
  },
  "devDependencies": {
    "@vitest/browser": "workspace:*",
    "vitest": "workspace:*"
  }
}
