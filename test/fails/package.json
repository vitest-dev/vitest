{
  "name": "@vitest/test-fails",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "jsdom": "^21.0.0",
    "vitest": "workspace:*"
  }
}
