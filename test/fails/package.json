{
  "name": "@vitest/test-fails",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@types/which": "^2.0.1",
    "execa": "^6.0.0",
    "vitest": "workspace:*",
    "which": "^2.0.2"
  }
}
