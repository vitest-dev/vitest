{
  "name": "@vitest/test-base",
  "private": true,
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "url": "^0.11.0",
    "vitest": "workspace:*"
  }
}
