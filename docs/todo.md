# UnoCSS Migration Todo

Attributify mode not working with new theme. Converting to class-based with `un-` prefix.

## Done

- [x] `ListItem.vue` - converted attributify to `un-` classes
- [x] `CRoot.vue` - added `un-` prefix
- [x] `CourseLink.vue` - converted attributify to `un-` classes
- [x] `FeaturesList.vue` - converted attributify to `un-` classes
- [x] `Advanced.vue` - added `un-` prefix
- [x] `Experimental.vue` - added `un-` prefix

## Remaining

None! All components migrated.

### Not used (skip)

- `HomePage.vue` - not used in new theme
